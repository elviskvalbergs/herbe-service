# Progress ledger — herbe.service Phase 1: Domain Core (Slice 1)
Plan: docs/superpowers/plans/2026-07-14-service-phase1-domain-core.md
Repo: ~/AI/herbe-service  Branch: feature/service-phase1-core (base: origin/phase0/foundations)
Worktree: ~/AI/herbe-service/.claude/worktrees/phase1-plan
Plan commit: fdce912
Scope: ERP-independent domain core (tree, orders, worksheets, status machine, charge type, coverage, HistoryEvent projector, seed). /api/ext read API + ERP push path = follow-on slices.

## Tasks
Task 1: complete (fdce912..1bd0cb5, review approved; reviewer re-ran tsc+eslint clean). types.ts (exact), 0008_item_models + 0009_service_items (order swapped — service_items FKs item_models; migrate.mjs sorts by filename), trigger reproduced, store tenant-scoped. Store test written (verbatim createTestDatabase pattern) but CI-only (no local PG). FIX 1bd0cb5: item_models got change_seq+trigger/updated_at/deleted_at (delta-syncable/tombstoneable; app-master, no erp scoping). Minor deferred: redundant label index (brief-specified).
Task 2: complete (1bd0cb5..5aaed63, review approved ✅; reviewer re-ran 7/7 + tsc + verified mapping vs docs 02:88/04:103). Pure logic. Minor deferred (all brief-inherited, cosmetic): FROM_INT should be Partial<Record>; redundant n===0 branch; inbound test only covers value 2.
Task 3: complete (5aaed63..183c7a3, review approved; reviewer re-ran + verified vs docs/11). Pure logic. FIX 183c7a3: fixed mislabeled empty-member test + added zero-coverage-of-4 / negative-n / empty-list-intersection cases (9->12 tests). impl untouched.
Task 4: complete (183c7a3..835f5dd, review approved after fix; reviewer re-ran tsc+eslint+splitter+drizzle-internals). Migrations 0010/0011/0012 (dep order, triggers verbatim), one-ws-per-(order×tech) partial unique idx, erp_refs set, stores tenant-scoped, InferSelectModel names deconflicted. FIX 835f5dd: getErpRefs now takes tenantId + filters (closed cross-tenant read gap, matches repo IDOR-fix convention). Store test CI-only. Minor deferred: unforced users relocation in schema.ts (harmless diff-noise).
Task 5: complete (835f5dd..1efceee, review approved; adjacency map verified edge-by-edge vs docs/02:92). FIX 1efceee: asserted the In-progress/Paused->Rejected fan-in edges + terminal cases (coverage % couldn't catch a dropped edge since canTransition is one statement). 7/7. impl untouched.
Task 6: complete (1efceee..92e308f, review approved, no Critical/Important; precedence+guards verified vs docs/02:64-72, reviewer re-ran 29/29+tsc). Precedence Closed>Invoiced>Cancelled>manual>derived; Confirmed needs >=1 Task 6 (order status machine): pendingTask 6 (order status machine): pending all Approved; canCancel/cascade share PRE_WORK constant. Minor -> FOLD INTO TASK 9: assert the 4 unexercised precedence PAIRINGS (Cancelled vs Work-done, Invoiced vs Confirmed, Confirmed vs Planned, Work-done vs In-progress) to lock the if-chain order.
Task 7: complete (92e308f..b9a8aa7, review approved, NO issues; reviewer re-ran 3/3 projector + tsc + verified keys/migration/FK conventions). Deterministic keys, group projection via resolveCoveredIds, dedupeByKey idempotent, append-only migration 0013 (no trigger), store onConflictDoNothing on (tenant_id,key), getHistoryForItem tenant-scoped. FK SET NULL on order_id/worksheet_id adjudicated CORRECT (matches 100% of repo schema convention; better than brief's plain-uuid). Store test CI-only.
Task 8: complete (b9a8aa7..faa09e7, review approved; reviewer traced FK concern to resolution + re-ran tsc/eslint). Full tree + orders in all 7 statuses + signed/Rejected/crew worksheets; setErpOwnedState seed/test-only; wired per-erpCompany into seedBaseline (which seeds a real users row for both tenants -> no FK violation). Determinism test correctly scopes to controlled fields not server-random PKs. DEFER TO BOOKINGS SLICE: Planned order is direct-written (no bookings entity exists yet); a future deriveOrderStatus recompute would yield New until a booking is seeded. Minor deferred: presence checks span both tenants not per-erpCompany; seed ws-update by id only.
Task 9: complete (faa09e7..007f91f, review approved, no issues; reviewer re-ran vitest --coverage [5 pure modules 100%] + tsc). Added lib/domain/** 90% override (no thresholds weakened), 4 precedence-pairing assertions locking the order-SM if-chain. Minor: one extra charge-type test beyond brief (disclosed, harmless).

ENV NOTE: no local Postgres (5432 closed, no .env.test). Pure-logic tests (charge-type/coverage/status-machines/projector-logic) run locally via vitest. DB-backed tests (stores, seed, migrations) are TDD-written but verified in CI only. tsc is the local gate for DB tasks. pnpm 11.9.0, node 26, vitest 3.2.7 all work in the worktree.
ALL 9 DOMAIN-CORE TASKS COMPLETE.

=== FINAL WHOLE-BRANCH REVIEW (opus) — "Ready to merge: Yes (optional fixes)" ===
NO Critical, NO Important. All 4 highest-risk cross-task invariants verified clean: migration sequence 0008-0013 applies end-to-end (all FKs satisfied, triggers correct, no dup names); Drizzle<->SQL agree on all 10 tables; status/charge-type vocabulary single-sourced + casing-consistent; seed FK-coherent + reaches each status validly; pure/IO separation clean; no out-of-scope leakage. Minors (all non-blocking): FROM_INT type + scan-export-untested + Accepted-set-only -> FIXED (ec16f48); Planned seed direct-write -> deferred to bookings slice; drizzle onDelete/default omitted in TS (cosmetic, DDL hand-authored).
POLISH ec16f48: FROM_INT Partial<Record>, scanServiceItemsForTenant CI-test, Accepted set-only comment.
=== SHIP STATUS ===
ALL 9 domain-core tasks + plan complete, per-task reviewed + final review clean. Pushed feature/service-phase1-core (HEAD ec16f48). Remote = GitHub-fetch/Bitbucket-push mirror -> branch on burti/herbe-service (Bitbucket). PR (Bitbucket, no gh): https://bitbucket.org/burti/herbe-service/pull-requests/new?source=feature/service-phase1-core -> target phase0/foundations (team's call re: base, given P0 not yet merged to preview + dual remote).
Pure lib/domain modules 100% coverage locally; DB store/seed tests CI-only (no local Postgres). Branch based on phase0/foundations (unmerged P0). NEXT SLICE (documented in plan): /api/ext/v1 read API (lights up the portal); then ERP inbound (WS3) + push saga (WS4) once the test ERP exists.

=== POST-MERGE CHECK (2026-07-14 resume) ===
P0 merged+healthy (preview 58f31fc + main 5701e75 both READY). P1 = OPEN PR #3 (feature/service-phase1-core->preview), was NOT merged — its feature-branch deploy ERRORed on the stale-base middleware->Edge issue (branch cut from phase0/foundations@86d4655, before proxy.ts fix a730f58). FIXED: merged origin/preview into the branch (clean, brought proxy.ts + .vercelignore + vercel.json; middleware.ts removed) -> pushed b4fa8d7 -> deploy rebuilding (should go green, PR #3 then mergeable).
POSTGRES: installed but @14 broken (ICU74 vs icu4c@78 mismatch); @17 works -> started on 5432, TEST_DATABASE_URL=postgres://elviskvalbergs@localhost:5432/postgres. ALL 9 domain+seed test files / 77 tests PASS vs live PG17 (migrations 0008-0013 apply clean; stores/seed/constraints verified). Permanent fix: brew reinstall postgresql@14 OR standardize @17 + .env.test.
NEXT SLICE STARTING: /api/ext/v1 read API (WS14 shell: scoped bearer tokens + rate limit; read endpoints producing frozen portal DTO shapes + 9->6 status projection). Now TDD-able vs live PG.

=== SLICE 2 PLANNED: /api/ext/v1 read API ===
Branch feature/service-phase1-ext-read (off domain-core b4fa8d7, stacks on PR #3). Plan: docs/superpowers/plans/2026-07-14-service-phase1-ext-read-api.md.
10 tasks: (1) service_items.customer_id + ext_tokens schema [KEY: digest found service_items had NO customer linkage — blocks /service-items?customerCodes]; (2) customer-scoped paginated store reads; (3) 9->6 status projection [pure]; (4) token hash + ext_tokens store; (5) bearer verify + narrow-never-widen scope; (6) token-keyed rate limit; (7) DTO mappers [pure]; (8) GET /service-items(+detail,history); (9) GET /orders(+detail); (10) admin mint + contract test + coverage gate.
Deferred: POST /requests|/confirm|/feedback (writes/Phase-2 entities), report PDFs, sites table, booking. Now TDD-able vs live PG (@14 on 5432, TEST_DATABASE_URL=postgres://elviskvalbergs@localhost:5432/postgres).
Task 1: complete (60ed656..f2593c9, review approved, NO issues; reviewer re-ran tsc + 8 DB tests vs live PG14). Migration 0014 (customer_id + ext_tokens), Drizzle, insertServiceItem customerId, seed links units/lots. Idempotent verified.
Task 3: complete (95bcf54..e8fa5da, approved, no issues; reviewer re-ran 10/10, mapping verbatim vs docs/08:71). toCustomerOrderStatus exhaustive Record 9->6.
Task 4: complete (e8fa5da..2d6c97a, approved, no Critical/Important; implementer STALLED pre-commit -> controller verified 14/14 + tsc + committed; reviewer re-ran 14/14+tsc+eslint). tokens.ts (generate/hash/constantTimeEqual) + ext_tokens store; raw NEVER persisted (structural: no column + behavioral test). findByHash intentionally returns revoked rows (Task 5 filters).
Task 5: complete (2d6c97a..27a4467, approved, no Critical/Important; reviewer re-ran 10/10 + traced scope branches). verifyExtRequest: 401 no_token/invalid_token/revoked (verifier is the only revoked gate), touch fire-and-forget, resolveScopeCodes narrow-never-widen (widen dropped, empty-req vs restricted returns token codes). Minor deferred: untested Bearer-whitespace cases (fail closed); reserved-unused 403.
Task 6: complete (27a4467..93f75bb, approved, no Critical/Important; reviewer re-ran 4/4 vs live PG + tsc + hand-verified migration idempotency). checkExtRateLimit fixed-60s-window, atomic ON CONFLICT increment, per-(token,endpoint,window) buckets, migration 0015. Minor deferred: default-policy branch untested; MAX literal duplicated; no row pruning.
DILIGENCE FIX c059dfe: baseline.test.ts was FAILING deterministically (on preview too, from domain-core PR#3) — seedDomain added customers/domain rows FKing erp_companies, but the test reset via delete(erpCompanies) which FK-violated. Fixed: TRUNCATE tenants CASCADE. FLAG USER: preview has this red test until slice-2 merges or c059dfe is cherry-picked.
Task 7: complete (c059dfe..d6964c9, approved, no blocking; reviewer diffed dto.ts byte-identical vs portal contract + re-ran 28/28 + tsc). dto.ts mirror + pure mappers; status always projected 6-value; timeline/invoices []; 4 required-field fallbacks (status/orderNumber/requestedAt/workDate) all real always-populated columns, disclosed. CARRY TO TASK 9: orderNumber->UUID fallback would look broken once a real create-order route exists (N/A now, seed sets it). Minor: model:{} edge untested; chargeType exported vs portal module-local.
Task 8: complete (d6964c9..78b78a9, approved after fixes; scope enforcement solid + DB-verified). 3 service-items GET routes + serviceItemListSchema + getServiceItemByLabelId/getItemModelsByIds. FIX 78b78a9: guard after-cursor (->400 invalid_cursor), validate id UUID (->404 not 500), getItemModelsByIds deletedAt filter. 14/14 route + full suite 341 green vs live PG. Implementer session-limit stalled pre-commit; controller committed 50994f5.
Task 9: complete (78b78a9..2b1520c, approved after fixes; scope + status-projection verified, reviewer re-ran 351/351). orders list+detail routes; ?status post-projection filter; input guards carried from Task 8. FIX 2b1520c: extracted+deduped resolveOrderServiceItems helper (DRY), ?status invalid ->400. 42/42 tests vs live PG.
Task 10: complete (2b1520c..a1292b4, approved, no Critical/Important; reviewer MUTATED dto.ts twice to prove contract test catches drift + re-ran 391/391 + coverage). admin ext-tokens mint route (ADMIN_EXT_TOKENS_SECRET, fail-closed, tenant derived from erpCompany, raw-once); contract test 21/21; coverage gate lib/api/ext/** + tokens.ts (additive). ext coverage 100/94/100/100. Minor: mint route doesn't check erpCompany.active (admin ops, low pri); 2 const-time helpers (pre-existing).
ALL 10 SLICE-2 TASKS DONE. Needs: ADMIN_EXT_TOKENS_SECRET env per environment.

=== SLICE 2 (/api/ext read API) — FINAL WHOLE-BRANCH REVIEW (opus) — "Ready to merge: Yes" ===
NO Critical/Important merge-blockers. All 3 headline risks CLEAR: auth->ratelimit->scope chain uniform across all 5 routes; DTO mirror matches portal contract field-for-field; migration chain 0014-0015 clean after domain-core 0001-0013 (FK targets exist, idempotent); every unconditional DTO field is NOT NULL. FIX 7dcf85d: getHistoryForItem ORDER BY coalesce(at,created_at) [customer-visible ordering]; admin 500 body static (no err leak). Deferred/noted: enum casts safe until ERP-inbound slice; erpCompany.active undefined in this slice; dangling doc-path comments; orderNumber->UUID fallback for future create-route.
Full suite 58 files/392 tests PASS vs live PG14; tsc clean; ext modules >=90% coverage.
SLICE 2 COMPLETE. Branch feature/service-phase1-ext-read (stacks on domain core in preview).
