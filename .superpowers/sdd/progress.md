# Progress ledger — herbe.service Phase 1: WS1 App shells + push
Plan: docs/superpowers/plans/2026-07-19-service-phase1-ws1-app-shells.md
Repo: ~/AI/herbe-service  Branch: feature/service-phase1-ws1-app-shells (base: origin/preview @ ba274f6)
Worktree: ~/AI/herbe-service/.claude/worktrees/ws1-app-shells
Plan commit: 988906d

(Prior content of this file — from the completed, merged domain-core + ext-read-api
slices on a different branch — is superseded and dropped; see doc 24 for that history.)

## Tasks
FINAL WHOLE-BRANCH REVIEW (opus, ba274f6..fba3b01) — "Ready to merge: with fixes", no Critical. 2 Important + minors fixed in f6c7a03: (1) unified the dead shadcn `.dark` variant onto `[data-theme="dark"]` (was inert — nothing set `.dark`); (2) both outbox routes auth()->getVerifiedSession (ERP-mutating, must honor revocation); + real root metadata, sunlight --fg-muted contrast (#565656 ~7.34:1), settings PATCH json try/catch. Deferred (documented in doc24 §4): scheme-switcher UI, field-shell role gate, admin-nav-additive question (WS2 role-model), React.cache(getVerifiedSession), push-toggle partial-PWA degradation, offline fallbacks.
MERGE + RECONCILE (87b3d1d merge origin/preview; c709f40 migration renumber): origin/preview advanced (WS4 ERP-outbound PR #13 landed, doc24 moved). Merged preview in. 6 conflicts: schema.ts/package.json/pnpm-lock (additive — kept both sides, regenerated lockfile), progress.md (kept mine). SEMANTIC: WS4 replaced the Phase-0 pushServiceOrderCreate spike (DELETED push-service-order.ts) with the push-queue saga + changed outbox payload to {orderId}. My Task 8 conflict-inbox retry was built on that spike -> broken. Resolved: took WS4's saga body for the main route (kept my getVerifiedSession), DELETED my now-dead lib/sync/outbox-push.ts, REWROTE app/api/sync/outbox/[id]/retry/route.ts to re-drive WS4's saga (enqueueOrderCreatePush+processPushQueue, {orderId}) mirroring the main route's proven tail (saga is idempotent so re-drive never duplicates). Reconciled both outbox test files (subagent): WS4's saga/{orderId} structure + my getVerifiedSession real-user sessions (bare {user:{id}} mocks 401 under getVerifiedSession). Renumbered my migrations 0020_user_settings->0022, 0021_push_subscriptions->0023 past WS4's 0020_erp_push_queue (branch-only/undeployed, runner keys on filename -> safe). POST-MERGE FULL GATE GREEN: vitest 961/961 (was 780 + WS4's tests), tsc/eslint/coverage clean.
Task 12 (wrap-up): doc24 updated — WS1 row flipped to done, new §2c infra section (the UI floor for WS9/WS10/WS2-frontend), §4 item 0 struck + WS1 deferred items added, §5 WS2-frontend-now-unblocked note, "Last updated" 2026-07-20, next migration = 0024. About to push + open PR into preview.
Task 11: complete (cfe6b54..b850c48, review approved after 1 hardening fix). Playwright smoke suite: playwright.config.ts (webServer boots `pnpm build`+`next start` so the real next-pwa SW exists; TEST_AUTH + DB env wired) + tests/e2e/global-setup.ts (seeds tech+dispatch personas with explicit roles — necessary because authorizeMagicLink's onConflictDoUpdate returns the existing row, so without pre-seed both would default to technician; shells out to migrate.mjs since Playwright's CJS transform can't import its import.meta.url guard) + tests/e2e/app-shell.spec.ts (4 tests: manifest served w/ real icons + theme_color, tech->field shell, dispatch->office shell, offline-reload-renders-chrome). Fixed the real manifest bug this task existed to catch: public/manifest.json had icons:[] (non-installable) + theme_color #000000 -> added real public/icons/icon-192.png + icon-512.png (square, forest-green, Principle 6 ok) + theme_color #134A40. vitest.config.ts excludes tests/e2e/**. RAN FOR REAL: 4/4 Playwright green against built server w/ live SW + Postgres + NextAuth; full vitest 780/780 still green w/ e2e excluded; tsc/eslint clean. FIX b850c48: (1) cache-priming race — added page.waitForFunction polling caches.open('pages') for /today before setOffline (page.reload resolves on load event, not on Workbox's waitUntil cache write); (2) added theme_color regression assertion; (3) added offline sync-chip assertion (data-state=offline, verified the testid/attr exist on the component). DEFERRED ITEM for doc24 §4: first-ever-visit-then-offline still hits the browser error page — next.config.ts's next-pwa has no `fallbacks` precached app-shell route, so the offline guarantee only holds for returning visits; a product-architecture decision (precache the shell / add a fallback route) for whoever owns offline resilience, out of WS1's scope.
Task 10: complete (123eab9..277ddc0, review APPROVED — reviewer independently confirmed the custom-worker bundling end-to-end by reading the on-disk public/sw.js's importScripts("/worker-<hash>.js") + hand-decoding urlBase64ToUint8Array against a known vector; no Critical/Important). MINOR DEFERRED (for final review triage): (1) getPushSubscriptionStatus() promise chain in PushToggle's effect has no .catch() + pushUnsupported() doesn't check 'PushManager' in window — a partial-PWA browser (SW+Notification but no Push API) would get an unhandled rejection instead of a graceful unsupported state; (2) two Minimal* SW interface return types are Promise<unknown> vs the spec's Promise<void>/Promise<WindowClient|null> (inconsequential, return values unused). Web Push client + service worker (docs/superpowers/sdd/task-10-brief.md), controller pre-verified customWorkerSrc against the installed node_modules/@ducanh2912/next-pwa/dist/index.d.ts (option name IS customWorkerSrc, default "worker", NOT the brief's guessed swSrc). worker/index.ts: created at repo root (default customWorkerSrc discovery, no next.config.ts change needed) with 'push' (parses JSON {title,body?,url?}, showNotification) and 'notificationclick' (close + focus an existing same-URL client or clients.openWindow) listeners. lib.dom vs lib.webworker's incompatible global `self` (this repo's tsconfig.json is lib:["dom",...] project-wide) solved with a small hand-written MinimalServiceWorkerGlobalScope/MinimalPushEvent/MinimalNotificationClickEvent/MinimalWindowClient interface set (not `any` — eslint's no-explicit-any is an error here) plus `export {}` to module-scope the `declare const self` shadow. VERIFIED END TO END, not just source-read: ran a real `pnpm build` (webpack) — next-pwa logged "Found a custom worker implementation at .../worker/index.ts" / "Building the custom worker to .../public/worker-<hash>.js", and the generated public/sw.js's define() callback opens with `importScripts("/worker-<hash>.js")` before workbox's own registrations, confirming the listeners genuinely run in the sw's global scope (build then failed later on an unrelated pre-existing DATABASE_URL requirement for page-data collection — not this task's concern). components/push-toggle.tsx: same ConflictInboxList/ConflictInbox split as Task 8 — enablePush/disablePush/getPushSubscriptionStatus/urlBase64ToUint8Array are plain async functions with no hooks, directly testable against mocked navigator.serviceWorker/pushManager/Notification globals (vi.stubGlobal); PushToggleView is a plain props-driven function (checking/unsupported/denied/subscribed/unsubscribed states) directly callable/inspectable; PushToggle (default export, 'use client') wires both with useState/useEffect — untested by direct invocation for the usual hooks-need-a-renderer reason. FIX post-eslint: (1) moved the "no vapidPublicKey" status derivation from a synchronous setState inside the mount effect into useState's lazy initializer — eslint's react-hooks/set-state-in-effect (error, not warning, in this repo's config) correctly flagged the original synchronous-setState-in-effect as an anti-pattern; (2) urlBase64ToUint8Array's return type pinned to Uint8Array<ArrayBuffer> (not bare Uint8Array, which widens to Uint8Array<ArrayBufferLike> and fails PushSubscriptionOptionsInit's BufferSource check under this TS/lib.dom version). NEXT_PUBLIC_VAPID_PUBLIC_KEY added to .env.example/.env.local (same value as VAPID_PUBLIC_KEY, duplicated under the client-visible prefix — VAPID_PRIVATE_KEY never duplicated this way) since PushToggle needs the key client-side; .gitignore/eslint.config.mjs extended for the new public/worker-*.js build artifact pattern (same story as existing sw.js/workbox-*.js entries). Wired <PushToggle vapidPublicKey={process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY ?? ''}> into a new "Notifications" section on both app/(field)/more/page.tsx (between Language and the coming-soon line) and the office settings stub (app/(office)/c/[companyId]/settings/page.tsx, Task 6's nav-vs-route-access warning comment left untouched). New tests: components/push-toggle.test.tsx 22 (urlBase64ToUint8Array decode against an independently-computed byte vector; getPushSubscriptionStatus's 4 states; enablePush's granted/default-then-granted/denied/requestPermission-denied/unsupported/request-failed paths incl. asserting subscribe called with the exact applicationServerKey and fetch called with the exact POST body; disablePush's unsubscribe+post/no-op-when-absent/unsupported paths; PushToggleView's 8 rendering cases). Extended app/(field)/more/page.test.tsx's existing wiring test with the new Notifications section assertion (element-tree destructure, not a new file, since More already had one). The generic office-stub-pages.test.tsx describe.each needed no change (still destructures only [heading, body], ignores the new 3rd child). Full suite 112 files / 780 tests passed, tsc + eslint clean (worker/index.ts is outside tsc's failure path since it type-checks under this project's lib.dom-only config, per the Minimal* interfaces above; it's also outside vitest.config.ts's coverage `include` glob, same as components/** generally, so no threshold applies to it or to push-toggle.tsx).
Task 9: complete (1b3b023..123eab9, review approved after 1 fix). Web Push infra: migration 0021 push_subscriptions (id/userId/endpoint unique/p256dh/auth/createdAt); POST /api/push/subscribe+unsubscribe (getVerifiedSession, unsubscribe scoped by endpoint+userId -- cross-user test proves foreign row survives); lib/push/send.ts sendPushToUser (real web-push package, 410/404 -> delete subscription, verified against web-push's actual WebPushError.statusCode source); POST /api/push/test follows THIS REPO'S ACTUAL admin-ops convention (bearer ADMIN_MIGRATIONS_SECRET + bearerMatches constant-time compare, same as run-migrations/ext-tokens routes -- NOT a session role check, confirmed by reading those routes directly before dispatch). Real VAPID keypair generated for .env.local (git-ignored, correct P-256/EC key lengths verified without printing values), .env.example ships empty placeholders + disclosed scope-limited header (repo's first env.example, only documents the 3 push vars, precedented by existing scoped .env.test.example). FIX 123eab9: (1) non-410/404 send failures now logged (statusCode+error) instead of silently counted -- diagnostic gap for the test route's whole purpose; (2) new test pins uniform-failure-across-all-subscriptions behavior (no throw, all attempted, correct failed count, none deleted) so a future fail-fast change can't silently regress it. 22+2 new tests, 757+/757+ suite, tsc/eslint clean.
Task 8: complete (ae381a6..1b3b023, review approved, no Critical/Important — retry-mechanism deviation from plan §2 decision 7's literal text independently verified real+justified by reading the original route + running the counter-based retry tests, which prove a genuine second push attempt not just a status flip). Conflict inbox (doc07 F10): lib/inbox/get-inbox-items.ts's getInboxItemsForUser(db, tenantId) — despite the name, tenant-scoped not per-user, since outbox_ops has no userId column (confirmed, same fact Task 7's briefcase "pending sync" bucket already noted) — reads failed outbox_ops rows only, mapped to a small InboxItem shape (id, entity, op, errorMessage, createdAt), sorted newest-first. Structured as collect-from-sources (array of one today) -> flat -> sort, not a plugin registry, so a second source (e.g. rejected-worksheet tasks once WS8/WS9 build that persistence) can be appended without a rewrite. RETRY MECHANISM (verified, not assumed): read app/api/sync/outbox/route.ts directly first — confirmed its idempotency check answers ANY pre-existing row (applied OR failed) as already_applied without re-pushing, so a naive re-POST would never actually retry a failed op. Extracted the push-and-record-outcome logic from that route's try/catch into lib/sync/outbox-push.ts's attemptOutboxPush(db, tenantId, opId, payload) (Phase 0 scope unchanged: hardcoded single-company-per-tenant lookup + SVOVc create only), refactored the original route to call it (behavior-preserving — its own 6 tests pass unmodified) and corrected its now-stale header comment, and added a dedicated app/api/sync/outbox/[id]/retry/route.ts: session-tenant-scoped (Task 16b's tenantId-from-session-only rule, same as the original route), 404 for a missing/foreign-tenant op id (no existence leak), 409 if the op's status isn't 'failed' (rejects retrying an already-applied op without re-pushing), otherwise resets status to 'pending' + clears errorMessage then calls attemptOutboxPush for a genuine second push attempt. components/conflict-inbox.tsx: same split as sync-status-chip.tsx/office-nav.tsx — ConflictInboxList is a plain, hook-free function (empty state "No conflicts.", one row per item with entity/op/timestamp/errorMessage + a Retry button, disabled+relabeled while in flight, per-item retry-error message) directly callable in tests; ConflictInbox (default export, 'use client') owns retryingId/retryError state and the actual fetch to the new retry route, calling router.refresh() after each attempt so a successfully-retried item's disappearance (no longer 'failed') is driven by the real server loader, not hand-maintained client state — untested by direct invocation for the same hooks-require-a-renderer reason SyncStatusChip/OfficeNavList aren't (no jsdom/RTL in this project). app/(field)/inbox/page.tsx: replaced the Task 5 "Coming soon." placeholder with getInboxItemsForUser(db, session.user.tenantId) piped into <ConflictInbox>; moved OUT of the shared app/(field)/field-stub-pages.test.tsx describe.each (same reasoning as Task 7's More) into its own DB-backed app/(field)/inbox/page.test.tsx. KNOWN LIMITATION (flagged per the brief, not solved): getInboxItemsForUser is tenant-scoped, not truly per-user — outbox_ops has no attribution column, so every technician in a tenant sees the same failed-ops list; fixing this needs outbox_ops (or whatever queues the write) to carry a userId, out of this task's scope. New tests: lib/inbox/get-inbox-items.test.ts 4 (empty, failed-only filter, cross-tenant exclusion, newest-first sort), app/api/sync/outbox/[id]/retry/route.test.ts 6 (401, successful retry with real re-push + DB row proof, still-fails-but-genuinely-retries proof via a push-call-counter, 404 missing id, 404 cross-tenant without leaking, 409 on non-failed status), components/conflict-inbox.test.tsx 6 (empty state, row shape incl. entity/op/timestamp/error text, enabled Retry button, onRetry called with correct id, in-flight disable is per-item not global, per-item retry-error message), app/(field)/inbox/page.test.tsx 3 (redirect, real end-to-end wiring, empty items array) = 19 new; field-stub-pages.test.tsx net -2 (Inbox's 2 cases removed, now covers 2 pages not 3, Jobs/Scan only). Full suite 107 files / 735 tests passed, tsc + eslint clean, coverage thresholds pass (lib/inbox/** and lib/sync/outbox-push.ts both 100%; overall 98.31% stmts).
Task 7: complete (a784780..ae381a6, review approved after 1 fix commit). FIX ae381a6: (1) LocaleSwitcher now actually rendered on More page (controller's dispatch had told implementer to skip it, contradicting the plan brief's "wire it in if Task 2 landed" -- resolved in favor of the plan since the precondition was genuinely met and the component is a zero-prop drop-in); (2) outbox_ops pending count switched from row-fetch+.length to sql`count(*)::int` (tenant-wide bucket, real scaling risk during sync degradation -- exactly when it needs to be cheap); scope_membership count left as row-fetch (fine, bounded per-user). Briefcase UI: components/briefcase-summary.tsx is a plain, hook-free, entity-agnostic presentational component — `buckets: {label, count, lastSyncedAt?}[]` prop, no fetching of its own (same directly-callable-in-tests convention as components/company-switcher.tsx). Renders one row per bucket, EXCEPT when the array is empty or every count is zero, in which case it renders a single "Your briefcase is empty." line (brief's explicit combined empty-state rule) — so a bucket with count 0 alongside a non-zero one still gets its own row (e.g. "Pending sync: 0" is real, useful information once at least one other bucket has data). lib/offline/briefcase-summary.ts (server-side data-loader, plain row-count selects rather than SQL count() to avoid postgres.js's string-vs-number count return-type ambiguity — repo has no existing count() precedent to match): buckets computed are (1) scope_membership rows for the signed-in userId with outScopeSeq IS NULL (not yet purged, Task 22's mechanism) and (2) outbox_ops rows for the session's tenantId with status='pending' — note this second bucket is tenant-wide, not user-scoped, since outbox_ops has no userId column (schema fact, not an oversight). DEXIE BUCKET DECISION (per brief's explicit either/or): skipped in this task, not wired as a third bucket. lib/offline/db.ts's OfflineDb is browser-only IndexedDB; folding it in would require a separate 'use client' sub-component reading Dexie and merging its count into the server-computed buckets client-side, which (a) this repo's testing convention can't directly unit-test the same way (hooks aren't callable-and-inspectable here, see components/locale-switcher.test.ts's precedent for why), and (b) has no clear product signal yet for what "briefcase" should mean for a cached-customers count specifically. Left as a noted follow-on wiring detail, not built speculatively (plan §2 decision 5 — no fabricated/half-real buckets). app/(field)/more/page.tsx: replaced the flat "Coming soon." body with a real "Your briefcase" section (h2 + <BriefcaseSummary>) plus ONE remaining "coming soon" line covering language/device-info/PIN-lock/sign-out — deliberately a single line rather than 4 stub rows, so Task 2's LocaleSwitcher and Task 10's push-toggle can each add their own section later without restructuring this page's layout (Task 2's standalone LocaleSwitcher is NOT wired in here — that's still that task's own call per its brief, unchanged from Task 5's placeholder note). Session-gated the same way as every other field route (getVerifiedSession -> redirect('/login')); buckets computed from session.user.id/session.user.tenantId, both already typed non-optional on Session (lib/auth/config.ts). TESTING: MorePage's DB-backed test moved OUT of the shared app/(field)/field-stub-pages.test.tsx describe.each (its element shape no longer matches the other 3 stubs) into its own app/(field)/more/page.test.tsx — session-gate redirect test + one wiring test (seeds a real scope_membership row via lib/sync/scope-membership.ts's enterScope, asserts the exact buckets array reaches <BriefcaseSummary> unchanged). Bucket-count correctness itself (purged rows excluded, non-pending ops excluded, cross-user/cross-tenant scoping) lives in lib/offline/briefcase-summary.test.ts, a dedicated DB-backed suite, not duplicated at the page level. New tests: components/briefcase-summary.test.tsx 4 (empty array, all-zero array, mixed-count rows incl. a zero-count row alongside a non-zero one, lastSyncedAt suffix), lib/offline/briefcase-summary.test.ts 3 (zero-rows default, scope_membership purge/cross-user exclusion, outbox_ops status/cross-tenant exclusion), app/(field)/more/page.test.tsx 2 (redirect, end-to-end wiring) = 9 new; field-stub-pages.test.tsx net -2 (More's 2 cases removed, now covers 3 pages not 4). Full suite 103 files / 718 tests passed, tsc + eslint clean.
Task 6: complete (0e93555..a784780, review approved after 2 fixes — layout-gate-blocks-page architecture independently verified sound via RSC execution-order reasoning). FIX a784780: (1) tenant-ownership check now also requires erp_companies.active=true (was leaking reachability of deactivated-but-same-tenant companies past the switcher's own active filter) -> unified notFound() for missing/foreign/inactive, test added; (2) all 7 remaining office nav-stub pages (dispatch/orders/worksheets/customers/service-items/stock/reports) got the same "nav visibility != route access control" warning comment settings/page.tsx already had, each citing its own OFFICE_NAV_SECTIONS capability. Deferred (noted, not blocking): Router Cache soft-navigation staleness on mid-session revocation is an inherent App-Router characteristic, not a regression -- flagged for awareness only. Office shell chrome: app/(office)/c/[companyId]/layout.tsx (async server component, no hooks of its own — same testable-by-calling-directly pattern as app/(field)/layout.tsx) now does the role+tenant gating Task 4's review flagged as missing on the placeholder page.tsx: getVerifiedSession -> redirect('/login'); role not in {dispatcher,back_office,admin} -> redirect('/'); companyId row missing OR its tenantId != session.user.tenantId -> notFound() (same 404 either way, no info leak). Centralized in the layout rather than duplicated across page.tsx + the 8 stub pages, since every route under the segment needs both checks and the layout already needs the tenant's company list for the switcher. Renders dark-forest sidebar (fixed --herbe-forest/white, NOT the theme-reactive --bg-inverse token — deliberately independent of the user's own light/dark/sunlight display-scheme preference, matching portal's .admin-sidebar hardcoding --brand-primary) + light content area, collapsing to a shadcn Sheet-based drawer under md (components/ui/sheet.tsx: vendored primitive, radix-ui's Dialog via the same combined-package convention as ui/button.tsx's Slot import, no dedicated test — same as button.tsx). components/office-nav.tsx: OFFICE_NAV_SECTIONS (8 sections, doc07 order) + getVisibleNavSections(role) gates each by hasCapability(role, capability) from lib/auth/roles.ts — verified against the real ROLE_CAPABILITIES table (not assumed): admin's capability set is settings-only (tenant:manage_settings etc.), so admin sees ONLY Settings/Admin, not the other 7 sections — matches the brief's own example literally. Two sections have no clean capability match, flagged rather than inventing one: Service items reuses 'customer:edit' (doc07 O7 groups Customers/sites/service-items as one back-office register set); Stock reuses 'item:edit' (closest existing match, judgment call). components/company-switcher.tsx: plain function (no hooks — active company passed as a prop), hidden when companies.length <= 1 (doc07 "hidden if single company"); companies resolved tenant-wide (same query shape as app/page.tsx's resolveDefaultCompanyId — confirmed no per-user company-access table exists), active-only, createdAt order. 8 nav-section stub pages (dispatch/orders/worksheets/customers/service-items/stock/reports/settings): honest "Coming soon." placeholders, NO session gate of their own (relies on the layout's — deliberate divergence from app/(field)'s per-page gate convention, noted in each file's header comment and in layout.tsx's own). page.tsx's pre-existing session-only gate left in place (redundant now, same known/deferred multi-getVerifiedSession-call tradeoff as the DEFERRED ITEM below) rather than removed — out of this task's brief scope, its header comment updated to stop claiming Task 6 would replace it wholesale. New tests: components/office-nav.test.ts 12 (section order/uniqueness, 3-contrasting-role capability gating incl. admin-sees-only-settings and technician/team_lead-see-nothing, active-route matching), components/company-switcher.test.tsx 3 (hidden at 1/0 companies, lists+marks-active at 2), app/(office)/c/[companyId]/layout.test.tsx 11 (DB-backed: no-session redirect, technician/team_lead redirect to '/', missing-company 404, foreign-tenant-company 404, shell renders for all 3 office roles, single- vs multi-company switcher props incl. inactive-company exclusion, Sheet drawer present alongside desktop aside), app/(office)/c/[companyId]/office-stub-pages.test.tsx 8 (no DB/auth mocking needed, unlike the field-shell equivalent, since these pages have no gate of their own) = 34 new. Full suite 100 files / 710 tests passed, tsc + eslint clean.
DEFERRED ITEM for doc24 §4 (not this slice's scope): wrap lib/auth/session-guard.ts's getVerifiedSession in React.cache() — root layout + page-level gates now each call it independently, 3x per-request DB round trips app-wide (functionally safe, no side effects to duplicate, but a cheap 2-line fix later).
Task 5: complete (064d199..41d130f + gap-fix 0e93555, review approved both rounds, no Critical). Field shell chrome: app/(field)/layout.tsx composes header (HerbeServiceLogo wordmark theme="light" matching the light --bg default + SyncStatusChip) + children + FieldTabBar. FieldTabBar: 5 tabs Today/Jobs/Scan/Inbox/More in doc07 order, isTabActive() handles nested routes correctly (verified), active state uses --product-service amber (not --accent/red, reserved brand+destructive — verified against doc14 §6 "app's accent/dot reaches for it", sound). SyncStatusChip: useSyncExternalStore for navigator.onLine (fixed from useState+useEffect double-render), honest offline/synced only, no fake pending-count (Task 7 wires that). 4 stub pages (jobs/scan/inbox/more) honest "Coming soon.", today/page.tsx got real empty-state text + kept session gate + team-lead switch note. GAP CAUGHT POST-REVIEW: Task 3 had left a handoff note ("Task 5 must wire displayScheme->data-theme/data-scheme") that the controller failed to relay into Task 5's dispatch — caught via manual verification before closing the task, fixed as 0e93555: app/layout.tsx now reads getVerifiedSession+getUserPrefs, maps displayScheme (dark->data-theme, sunlight->data-scheme, standard/no-session->neither) onto <html>, server-rendered no client flash. i18n NOT wired for new chrome/placeholder strings (matches Task 4's own precedent, verified true). 18+4(5b) new tests, 678/678 full suite, tsc/eslint clean. Field shell chrome: app/(field)/layout.tsx is a plain server component with no hooks of its own (composes header — wordmark + <SyncStatusChip/> — + children + <FieldTabBar/>), kept hook-free deliberately so it stays testable by calling FieldLayout() directly and asserting element-type identity (FieldTabBar/SyncStatusChip referenced only as types, never invoked — same pattern app/layout.test.tsx uses for NextIntlClientProvider). components/field-tab-bar.tsx: 'use client', usePathname()+next/link, 5 tabs in doc07 order (Today/Jobs/Scan/Inbox/More); pure FIELD_TABS + isTabActive() exported for hook-free testing (isTabActive matches nested routes, e.g. /jobs/123 keeps Jobs active). Active-tab color uses --product-service (amber, doc14 §6 "reads field/hi-vis") rather than --accent (herbe-red is brand-mark + destructive only, suite non-negotiable per doc07) — a design call flagged for design review. components/sync-status-chip.tsx: 'use client', useState/useEffect + navigator.onLine + online/offline listeners; pure deriveSyncStatus(online) exported — offline maps to --sync-local/CloudOff ("stored on this device only", the correct semantic for zero connectivity), the online placeholder maps to --sync-synced/CloudCheck. SCOPE DECISION (per brief's explicit allowance): no pending-N-ops or syncing state wired — no client-side outbox exists yet to count against (lands with Task 7's briefcase loader); wiring a fake count would violate plan §2 decision 5. Route stubs app/(field)/{jobs,scan,inbox,more}/page.tsx: identical getVerifiedSession -> redirect('/login') gate as today/page.tsx (no centralized route guard in this repo), honest "Coming soon." body, no fake data. app/(field)/today/page.tsx: kept its session gate + team-lead switch note (judgment call: note stays on Today rather than moving to More — More has zero real content this task, and bolting one substantive paragraph onto an otherwise "coming soon" page would blur its own placeholder signal; More remains the note's intended eventual home once it has something real to point at), replaced "Field shell placeholder" text with an honest "No jobs scheduled for today yet." empty state. i18n NOT wired for the new chrome/placeholder strings (tab labels, header, coming-soon text) — matches Task 4's own precedent (its Home/NoCompanyAccess placeholder strings are hardcoded English too, despite doc07's "all strings through i18n" aspiration); flagged as a gap for whichever task gives these surfaces real content. TESTING: field-tab-bar.tsx/sync-status-chip.tsx use hooks (usePathname/useState) so aren't directly callable per this project's no-jsdom convention (see components/locale-switcher.test.ts); same next/navigation + next/link mocks as prior tasks, plus a new lucide-react mock (its module-level React.createContext call crashes under the forced react-server condition, same class of issue as next-intl). The 4 near-identical stub pages are consolidated into one app/(field)/field-stub-pages.test.tsx (one test DB) rather than 4 separate DB-backed files, since there's nothing page-specific to isolate beyond title text. New tests: layout.test.tsx 2, field-tab-bar.test.ts 6, sync-status-chip.test.ts 2, field-stub-pages.test.tsx 8 (4 pages x 2) = 18 new. Full suite 96 files / 674 tests passed, tsc clean, coverage thresholds pass (98.21% stmts overall; app/(field)/** at 100%; components/** isn't in the coverage include glob so isn't gated by it).
Task 4: complete (9bee763..064d199, review approved, no Critical/Important). app/page.tsx: getVerifiedSession -> no session ->/login; role branch: technician/team_lead->/today (team_lead confirmed field-only per docs/05-users-auth.md:36 + roles.ts capabilities, static non-functional switch-note only); dispatcher/back_office/admin -> resolveDefaultCompanyId (tenant-wide active erp_companies query, verified no per-user company-access table exists, cross-tenant-leak test included) -> /c/[companyId] or NoCompanyAccess if zero. Placeholders created: app/(field)/today/page.tsx, app/(office)/c/[companyId]/page.tsx (both trivial, session-gated, no role/tenant check on the office one yet -- reviewer confirmed inert/non-exploitable since page does no DB read keyed by companyId; Task 6 must add tenant-ownership + role gate when real content lands). 14 new tests, 656/656 suite, tsc/eslint clean.
Task 1: complete (e582fc4..e9f1fbe, review approved, no Critical/Important). Design tokens (app/design-tokens.css: base palette subset + status/comfy-tier copied from herbe-design-system/tokens.css, --product-service amber, --ui-field-* density, sunlight scheme contrast-verified ~7.6:1/~8.2:1, sync-state/charge-type/work-entry-mode/revision-state tokens, print tokens — zero border-radius declarations, Principle 6 test-enforced). components/herbe-service-logo.tsx (amber square dot, inline SVG, letterSpacing via style not SVG attr). Poppins TTFs copied byte-identical from portal. shadcn init done via new CLI presets (`-b radix -p nova`, registry changed since plan was written — verified real, not an excuse) -> components.json "style":"radix-nova". pnpm-workspace.yaml ignoreWorkspaceRootCheck:true added (documented) for shadcn's pnpm add. Geist fully removed. 20/20 tests, tsc/eslint clean. Minor deferred: no visual/screenshot check that "service" text doesn't clip its computed viewBox width (structural test only, math verified independently by reviewer).
Task 2: complete (e9f1fbe..eb683f7, review approved, no Critical/Important — middleware.ts->proxy.ts deviation independently re-verified by reviewer via commit a730f58 + doc24 + next-intl source, confirmed real not excuse). DEVIATION FROM BRIEF (verified, not guessed): brief said "create middleware.ts (doesn't exist yet)" but that's stale — proxy.ts already exists (a730f58, Next 16's middleware->proxy rename, doc24 confirms it's P0 substrate) and already did the cookie-read/validate/fallback/header-forward job. Creating middleware.ts would've been dead code at best, a regression of the just-fixed Edge-runtime __dirname bug at worst. Adapted proxy.ts instead: replaced its inlined locale-list duplicate with the canonical `@/lib/i18n/config` import, named the header constant `INTL_LOCALE_HEADER` ('X-NEXT-INTL-LOCALE', verified against next-intl@4.13.1 source + matches herbe-portal's constant). app/layout.tsx now async, calls next-intl/server's getLocale()/getMessages(), wraps children in NextIntlClientProvider, `<html lang>` dynamic. components/locale-switcher.tsx: standalone 'use client', useLocale() for current value (no prop needed), sets NEXT_LOCALE cookie + router.refresh(); cookie-building logic extracted as pure `buildLocaleCookie` (hooks aren't unit-testable here — no jsdom/RTL in this project, same constraint that already excludes app/layout.tsx from coverage). locales/*.json seeded with a real `{app:{name:"herbe.service"}}` key (were placeholder `{}`) — needed to prove a translated string actually flows through, not a stub. TESTING WALL (verified empirically, cost most of the session): this project's vitest.config.ts forces the 'react-server' resolve condition globally so lib/i18n/request.ts resolves correctly; that same setting makes `next-intl`'s real NextIntlClientProvider (and next/navigation's useRouter) crash on import (`createContext is not a function` — client-only React API stripped from the react-server build) and makes next-intl/server's cache()-wrapped getLocale()/getConfig() hang indefinitely when called directly (no real Next.js request/RSC context to back React's cache()). Root-caused via isolated probes, not assumed. Worked around per-test: `vi.mock('next-intl', ...)`/`vi.mock('next/navigation', ...)` stub only the client-boundary pieces (not next-intl's own logic — that's next-intl's problem, not ours); `vi.mock('next-intl/config', ...)` + `vi.importActual` (plain dynamic import() inside the factory deadlocks, importActual doesn't) lets the REAL lib/i18n/request.ts + REAL locales/en.json run end-to-end. 20 new tests (proxy.test.ts 10, app/layout.test.tsx 2, components/locale-switcher.test.ts 8), all green; full suite 357/630 passed (273 skipped) + pre-existing 44 DB-dependent files failing on missing TEST_DATABASE_URL — confirmed via git-stash baseline diff that this is 100% pre-existing (same 44 fail on HEAD before this task's changes), zero new failures. tsc + eslint clean.
Task 3: complete (eb683f7..9bee763, review approved after 1 fix — users-table precedent + rejection-not-coercion independently verified). FIX 9bee763: invalid-displayScheme route test now has the same follow-up-GET persisted-state assertion as the invalid-locale test (was status-only, asymmetric). Settings model (user prefs): locale + display_scheme added directly to `users` (scripts/migrations/0020_user_settings.sql, idempotent ADD COLUMN IF NOT EXISTS) rather than a separate table — matches this table's existing convention of per-user preference-style columns (password_hash/mfa_* landed the same way in 0019). drizzle/schema.ts users table extended with the two columns, defaults 'lv'/'standard'. lib/settings/user-prefs.ts: getUserPrefs/setUserPrefs, validating locale against lib/i18n/config.ts's isLocale and displayScheme against a new 3-value union (isDisplayScheme) — invalid values throw, never coerced; setUserPrefs supports partial updates (only the given field(s) change). app/api/settings/route.ts: GET/PATCH via getVerifiedSession (lib/auth/session-guard), 401 unauthenticated, 400 on invalid body (non-string field or a string that fails validation). 12 new tests (lib/settings/user-prefs.test.ts 6: defaults, unknown-user rejection, full round-trip, partial update, invalid-locale/invalid-scheme rejected without persisting; app/api/settings/route.test.ts 6: 401 GET/PATCH, defaulted GET, valid PATCH persists + round-trips through GET, invalid locale/scheme -> 400 with prefs left unchanged) — all DB-backed against real Postgres per this repo's harness convention (lib/test-support/db.ts). Full suite 642/642 passed, tsc clean. No scope creep: no tenant-admin theming UI, no extra settings fields beyond the two the brief specified.
Task 4: complete, review pending. Role-based root routing: app/page.tsx calls getVerifiedSession(db) then branches on session.user.role (lib/auth/roles.ts) — no session -> /login (page doesn't exist yet in this repo, confirmed known WS1/WS9 frontend gap per docs/24 §5, out of this task's scope to build); technician|team_lead -> /today; dispatcher|back_office|admin -> /c/{companyId} via resolveDefaultCompanyId(tenantId) (oldest-createdAt active erp_companies row for the tenant — no per-user company-access table exists in this repo today, erp_companies scopes to tenantId only, so tenant-wide access IS the real current model; per-user narrowing is a gap for a later WS, noted not solved). Zero-active-companies renders an inline NoCompanyAccess message rather than redirecting to a new route (real edge case, no further route needed for this task). team_lead: re-checked docs/05-users-auth.md's role table — team_lead has technician capabilities + team management within the field context only, none of dispatcher's (order:view_all, dispatch:manage, etc. — lib/auth/roles.ts ROLE_CAPABILITIES confirms), so it redirects to the same field-shell target as technician, not into office. The doc 07 "switch to office" affordance for dual-hat users is implemented as an honest, non-functioning note (`data-testid="team-lead-switch-note"`) inside the /today placeholder, not a real shell-switch — there's nothing on the office side for team_lead to switch into yet. PLACEHOLDERS CREATED for Tasks 5/6 (both session-gated via getVerifiedSession, genuinely minimal — no real chrome): `app/(field)/today/page.tsx`, `app/(office)/c/[companyId]/page.tsx`. 14 new tests (app/page.test.tsx 9: all 5 roles + no-session + company ordering/inactive/cross-tenant + zero-company edge case; app/(field)/today/page.test.tsx 3: no-session redirect + switch-note presence/absence; app/(office)/c/[companyId]/page.test.tsx 2: no-session redirect + companyId render), all DB-backed against real Postgres. Full suite 92 files / 656 tests passed, tsc + eslint clean. vitest.config.ts: removed `app/page.tsx` from the coverage exclude list — it now has real branch logic and a real test file, unlike the create-next-app boilerplate that justified excluding it.
